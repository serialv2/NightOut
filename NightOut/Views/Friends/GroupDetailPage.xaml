<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="NightOut.Views.Friends.GroupDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.NavBarIsVisible="False"
    Shell.TabBarIsVisible="False"
    BackgroundColor="#05070D">

    <Grid BackgroundColor="#05070D">
        <ScrollView x:Name="GroupScrollView">
            <VerticalStackLayout IsVisible="{Binding HasSelectedGroup}" Spacing="0">

                <!-- BANNIERE + IDENTITE GROUPE -->
                <Grid HeightRequest="260">

                    <Image Source="{Binding SelectedGroup.PhotoUrl}"
                           Aspect="AspectFill"
                           Opacity="1" />

                    <BoxView BackgroundColor="#000000"
                             Opacity="0.25" />

                    <Grid Padding="18,20,18,0"
                          ColumnDefinitions="Auto,*,Auto,Auto"
                          VerticalOptions="Start">

                        <Button Text="‹"
                                FontSize="28"
                                WidthRequest="48"
                                HeightRequest="48"
                                CornerRadius="24"
                                BackgroundColor="#66000000"
                                TextColor="White"
                                Padding="0"
                                Clicked="OnBackClicked" />

                        <Button Grid.Column="2"
                                Text="↗"
                                FontSize="22"
                                WidthRequest="48"
                                HeightRequest="48"
                                CornerRadius="24"
                                BackgroundColor="#66000000"
                                TextColor="White"
                                Padding="0"
                                Command="{Binding ShareSelectedGroupCommand}" />

                        <Button Grid.Column="3"
                                Text="⋯"
                                FontSize="26"
                                WidthRequest="48"
                                HeightRequest="48"
                                CornerRadius="24"
                                BackgroundColor="#66000000"
                                TextColor="White"
                                Padding="0"
                                Margin="8,0,0,0" />
                    </Grid>

                    <VerticalStackLayout VerticalOptions="End"
                                         Padding="24,0,24,24"
                                         Spacing="7">

                        <Label Text="{Binding SelectedGroup.DisplayTitle}"
                               TextColor="White"
                               FontSize="32"
                               FontAttributes="Bold"
                               LineBreakMode="TailTruncation"
                               MaxLines="1" />

                        <Label Text="{Binding SelectedGroupMetaLabel}"
                               TextColor="#D1D5DB"
                               FontSize="15"
                               LineBreakMode="TailTruncation" />

                        <HorizontalStackLayout Spacing="12">
                            <Label Text="👥"
                                   FontSize="16" />

                            <Label Text="{Binding SelectedGroupMemberLabel}"
                                   TextColor="#E5E7EB"
                                   FontSize="15" />

                            <Label Text="●"
                                   TextColor="#22C55E"
                                   FontSize="15" />

                            <Label Text="{Binding SelectedGroupOnlineLabel}"
                                   TextColor="#E5E7EB"
                                   FontSize="15" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                </Grid>

                <!-- ACTIONS PRINCIPALES -->
                <Grid Padding="16,18,16,8" ColumnDefinitions="*,*" ColumnSpacing="12">
                    <Button Text="☷  Inviter des amis"
                            BackgroundColor="#121722"
                            TextColor="#F9FAFB"
                            BorderColor="#2A3143"
                            BorderWidth="1"
                            CornerRadius="18"
                            HeightRequest="58"
                            Command="{Binding ShareSelectedGroupCommand}" />

                    <Button Grid.Column="1"
                            Text="📅  Nouvelle sortie"
                            BackgroundColor="#7C3AED"
                            TextColor="White"
                            CornerRadius="18"
                            HeightRequest="58"
                            Clicked="OnGroupSortiesClicked" />
                </Grid>

                <!-- NAVIGATION GROUPE UNIQUE -->
                <!-- CONTENU GROUPE -->
                <Frame Margin="16,0,16,12" Padding="0" CornerRadius="22" HasShadow="False" BorderColor="#1F2635" BackgroundColor="#10131B">
                    <VerticalStackLayout Spacing="0">
                        <Grid Padding="8,8" ColumnDefinitions="*,*,*,*,*" ColumnSpacing="0">
                            <Button x:Name="BtnGroupDiscussion" Text="Messages" FontSize="13" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="#C084FC" Clicked="OnGroupDiscussionClicked"/>
                            <Button x:Name="BtnGroupSorties" Grid.Column="1" Text="Sorties" FontSize="13" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="#9CA3AF" Clicked="OnGroupSortiesClicked"/>
                            <Button x:Name="BtnGroupMedias" Grid.Column="2" Text="Médias" FontSize="13" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="#9CA3AF" Clicked="OnGroupMediasClicked"/>
                            <Button x:Name="BtnGroupMembres" Grid.Column="3" Text="Membres" FontSize="13" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="#9CA3AF" Clicked="OnGroupMembresClicked"/>
                            <Button x:Name="BtnGroupParametres" Grid.Column="4" Text="Réglages" FontSize="13" FontAttributes="Bold" BackgroundColor="Transparent" TextColor="#9CA3AF" Clicked="OnGroupParametresClicked"/>
                        </Grid>

                        <BoxView HeightRequest="1" Color="#242B3A"/>

                        <!-- DISCUSSION -->
                        <VerticalStackLayout x:Name="GroupSectionDiscussion" Padding="14" Spacing="10">
                            <CollectionView ItemsSource="{Binding SelectedGroupMessages}" HeightRequest="310" BackgroundColor="Transparent">
                                <CollectionView.EmptyView>
                                    <Label Text="Aucun message. Lance la discussion 🍻" TextColor="#6B7280" HorizontalOptions="Center" Margin="0,24"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="46,*,Auto" Padding="0,8" ColumnSpacing="10">
                                            <Frame WidthRequest="42" HeightRequest="42" CornerRadius="21" Padding="0" BackgroundColor="#1A1F2B" BorderColor="Transparent" IsClippedToBounds="True">
                                                <Image Source="{Binding Sender.AvatarUrl}" Aspect="AspectFill"/>
                                            </Frame>

                                            <VerticalStackLayout Grid.Column="1" Spacing="3">
                                                <HorizontalStackLayout Spacing="7">
                                                    <Label Text="{Binding SenderName}" TextColor="#F9FAFB" FontAttributes="Bold"/>
                                                    <Label Text="{Binding TimeLabel}" TextColor="#8B94A7" FontSize="12"/>
                                                </HorizontalStackLayout>

                                                <Label Text="{Binding MessageText}" TextColor="#E5E7EB" FontSize="14" IsVisible="{Binding HasText}"/>
                                                <Image Source="{Binding PhotoUrl}" Aspect="AspectFill" HeightRequest="190" IsVisible="{Binding HasPhoto}"/>

                                                <Button Text="🎥 Ouvrir la vidéo"
                                                        BackgroundColor="#1A1F2B"
                                                        TextColor="#F9FAFB"
                                                        CornerRadius="12"
                                                        IsVisible="{Binding HasVideo}"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenMessageMediaCommand}"
                                                        CommandParameter="{Binding .}"/>
                                            </VerticalStackLayout>

                                            <Label Grid.Column="2" Text="💜" FontSize="18" TextColor="#A855F7" VerticalOptions="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>

                            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                <Entry Placeholder="Écrire au groupe..."
                                       PlaceholderColor="#8B94A7"
                                       TextColor="#F9FAFB"
                                       BackgroundColor="#1A1F2B"
                                       Text="{Binding NewGroupMessage}"/>

                                <Button Grid.Column="1"
                                        Text="Envoyer"
                                        BackgroundColor="#7C3AED"
                                        TextColor="White"
                                        CornerRadius="12"
                                        Command="{Binding SendGroupMessageCommand}"/>
                            </Grid>

                            <HorizontalStackLayout Spacing="8">
                                <Button Text="📷 Photo" BackgroundColor="#1A1F2B" TextColor="#F9FAFB" CornerRadius="12" Command="{Binding SendGroupPhotoCommand}" CommandParameter="camera"/>
                                <Button Text="🖼️ Galerie" BackgroundColor="#1A1F2B" TextColor="#F9FAFB" CornerRadius="12" Command="{Binding SendGroupPhotoCommand}" CommandParameter="gallery"/>
                                <Button Text="🎥 Vidéo" BackgroundColor="#1A1F2B" TextColor="#F9FAFB" CornerRadius="12" Command="{Binding SendGroupVideoCommand}" CommandParameter="gallery"/>
                            </HorizontalStackLayout>
                        </VerticalStackLayout>

                        <!-- SORTIES -->
                        <VerticalStackLayout x:Name="GroupSectionSorties" Padding="14" Spacing="12" IsVisible="False">
                            <Label Text="Proposer une soirée" TextColor="#F9FAFB" FontSize="18" FontAttributes="Bold"/>

                            <Frame Padding="14" CornerRadius="18" HasShadow="False" BorderColor="#242B3A" BackgroundColor="#121722">
                                <VerticalStackLayout Spacing="10">
                                    <Entry Placeholder="Titre" PlaceholderColor="#8B94A7" TextColor="#F9FAFB" BackgroundColor="#1A1F2B" Text="{Binding OutingTitle}"/>

                                    <Label Text="Bar de la soirée" TextColor="#D1D5DB" FontAttributes="Bold"/>

                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                        <Entry Placeholder="Rechercher un bar, une ville, une adresse..."
                                               PlaceholderColor="#8B94A7"
                                               TextColor="#F9FAFB"
                                               BackgroundColor="#1A1F2B"
                                               Text="{Binding OutingBarSearchQuery}"
                                               ReturnCommand="{Binding SearchOutingBarsCommand}"/>
                                        <Button Grid.Column="1"
                                                Text="🔎"
                                                BackgroundColor="#7C3AED"
                                                TextColor="White"
                                                CornerRadius="12"
                                                Command="{Binding SearchOutingBarsCommand}"/>
                                    </Grid>

                                    <Label Text="{Binding SelectedOutingBarLabel}"
                                           TextColor="#C084FC"
                                           FontAttributes="Bold"/>

                                    <CollectionView ItemsSource="{Binding AvailableBars}"
                                                    SelectionMode="None"
                                                    HeightRequest="180">
                                        <CollectionView.EmptyView>
                                            <Label Text="Recherche un bar par nom ou par ville. Les suggestions proches apparaîtront ici si le GPS trouve des bars."
                                                   TextColor="#8B94A7"
                                                   FontSize="13"
                                                   Margin="0,8"/>
                                        </CollectionView.EmptyView>
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate>
                                                <Frame Padding="10"
                                                       Margin="0,4"
                                                       CornerRadius="12"
                                                       HasShadow="False"
                                                       BackgroundColor="#1A1F2B"
                                                       BorderColor="#242B3A">
                                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                                                        <VerticalStackLayout Spacing="2">
                                                            <Label Text="{Binding Name}"
                                                                   TextColor="#F9FAFB"
                                                                   FontAttributes="Bold"/>
                                                            <Label Text="{Binding Address}"
                                                                   TextColor="#9CA3AF"
                                                                   FontSize="12"
                                                                   LineBreakMode="TailTruncation"/>
                                                        </VerticalStackLayout>

                                                        <Button Grid.Column="1"
                                                                Text="Choisir"
                                                                BackgroundColor="#7C3AED"
                                                                TextColor="White"
                                                                CornerRadius="12"
                                                                Padding="12,6"
                                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ChooseOutingBarCommand}"
                                                                CommandParameter="{Binding .}"/>
                                                    </Grid>
                                                </Frame>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>

                                    <HorizontalStackLayout Spacing="8">
                                        <DatePicker Date="{Binding OutingDate}" TextColor="#F9FAFB" BackgroundColor="#1A1F2B"/>
                                        <TimePicker Time="{Binding OutingTime}" TextColor="#F9FAFB" BackgroundColor="#1A1F2B"/>
                                    </HorizontalStackLayout>

                                    <Editor Placeholder="Message pour le groupe"
                                            PlaceholderColor="#8B94A7"
                                            TextColor="#F9FAFB"
                                            BackgroundColor="#1A1F2B"
                                            HeightRequest="80"
                                            Text="{Binding OutingMessage}"/>

                                    <Button Text="Lancer l'invitation de soirée"
                                            BackgroundColor="#7C3AED"
                                            TextColor="White"
                                            CornerRadius="12"
                                            Command="{Binding CreateGroupOutingCommand}"/>
                                </VerticalStackLayout>
                            </Frame>

                            <Label Text="Sorties du groupe" TextColor="#F9FAFB" FontSize="18" FontAttributes="Bold"/>

                            <CollectionView ItemsSource="{Binding SelectedGroupOutings}" HeightRequest="390" SelectionMode="None">
                                <CollectionView.EmptyView>
                                    <Label Text="Aucune sortie proposée" TextColor="#6B7280" HorizontalOptions="Center" Margin="0,24"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Frame Padding="14" Margin="0,6" CornerRadius="18" HasShadow="False" BorderColor="#242B3A" BackgroundColor="#121722">
                                            <VerticalStackLayout Spacing="10">
                                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                                                    <VerticalStackLayout Spacing="5">
                                                        <Label Text="{Binding Title}" TextColor="#F9FAFB" FontAttributes="Bold" FontSize="16"/>
                                                        <Label Text="{Binding BarName}" TextColor="#C084FC"/>
                                                        <Label Text="{Binding PlannedLabel}" TextColor="#8B94A7" FontSize="12"/>
                                                        <Label Text="{Binding Message}" TextColor="#E5E7EB"/>
                                                        <Label Text="{Binding StatsLabel}" TextColor="#F9FAFB" FontAttributes="Bold"/>
                                                    </VerticalStackLayout>

                                                    <Button Grid.Column="1"
                                                            Text="Voir"
                                                            BackgroundColor="#4C1D95"
                                                            TextColor="White"
                                                            CornerRadius="14"
                                                            Padding="12,6"
                                                            VerticalOptions="Center"
                                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ViewOutingBarCommand}"
                                                            CommandParameter="{Binding .}"/>
                                                </Grid>

                                                <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8">
                                                    <Button Grid.Column="0"
                                                            Text="✅ J'y vais"
                                                            BackgroundColor="#1A1F2B"
                                                            TextColor="#F9FAFB"
                                                            CornerRadius="12"
                                                            FontSize="12"
                                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RespondYesToOutingCommand}"
                                                            CommandParameter="{Binding .}"/>

                                                    <Button Grid.Column="1"
                                                            Text="🤔 Peut-être"
                                                            BackgroundColor="#1A1F2B"
                                                            TextColor="#F9FAFB"
                                                            CornerRadius="12"
                                                            FontSize="12"
                                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RespondMaybeToOutingCommand}"
                                                            CommandParameter="{Binding .}"/>

                                                    <Button Grid.Column="2"
                                                            Text="❌ Non"
                                                            BackgroundColor="#1A1F2B"
                                                            TextColor="#F9FAFB"
                                                            CornerRadius="12"
                                                            FontSize="12"
                                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RespondNoToOutingCommand}"
                                                            CommandParameter="{Binding .}"/>
                                                </Grid>

                                                <Button Text="Partager cette sortie"
                                                        BackgroundColor="#1A1F2B"
                                                        TextColor="#F9FAFB"
                                                        CornerRadius="12"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ShareOutingCommand}"
                                                        CommandParameter="{Binding .}"/>
                                            </VerticalStackLayout>
                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>

                        <!-- MEDIAS -->
                        <VerticalStackLayout x:Name="GroupSectionMedias" Padding="14" Spacing="12" IsVisible="False">
                            <Label Text="Médias du groupe" TextColor="#F9FAFB" FontSize="18" FontAttributes="Bold"/>

                            <CollectionView ItemsSource="{Binding SelectedGroupMessages}" HeightRequest="340">
                                <CollectionView.EmptyView>
                                    <Label Text="Aucun média pour l'instant" TextColor="#6B7280" HorizontalOptions="Center" Margin="0,24"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Frame Padding="10" Margin="0,6" CornerRadius="18" BackgroundColor="#121722" BorderColor="#242B3A" HasShadow="False" IsVisible="{Binding HasMedia}">
                                            <VerticalStackLayout Spacing="6">
                                                <Image Source="{Binding PhotoUrl}" Aspect="AspectFill" HeightRequest="210" IsVisible="{Binding HasPhoto}"/>

                                                <Button Text="🎥 Ouvrir la vidéo"
                                                        BackgroundColor="#1A1F2B"
                                                        TextColor="#F9FAFB"
                                                        CornerRadius="12"
                                                        IsVisible="{Binding HasVideo}"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenMessageMediaCommand}"
                                                        CommandParameter="{Binding .}"/>

                                                <Label Text="{Binding SenderName}" TextColor="#F9FAFB" FontAttributes="Bold"/>
                                                <Label Text="{Binding TimeLabel}" TextColor="#8B94A7" FontSize="12"/>
                                            </VerticalStackLayout>
                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>

                            <HorizontalStackLayout Spacing="8">
                                <Button Text="📷 Appareil photo" BackgroundColor="#7C3AED" TextColor="White" CornerRadius="12" Command="{Binding SendGroupPhotoCommand}" CommandParameter="camera"/>
                                <Button Text="🖼️ Galerie" BackgroundColor="#1A1F2B" TextColor="#F9FAFB" CornerRadius="12" Command="{Binding SendGroupPhotoCommand}" CommandParameter="gallery"/>
                            </HorizontalStackLayout>
                        </VerticalStackLayout>

                        <!-- MEMBRES -->
                        <VerticalStackLayout x:Name="GroupSectionMembres" Padding="14" Spacing="12" IsVisible="False">
                            <Label Text="Membres du groupe" TextColor="#F9FAFB" FontSize="18" FontAttributes="Bold"/>

                            <CollectionView ItemsSource="{Binding SelectedGroupMembers}" HeightRequest="240">
                                <CollectionView.EmptyView>
                                    <Label Text="Aucun membre affiché" TextColor="#6B7280" HorizontalOptions="Center" Margin="0,24"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="46,*,Auto" Padding="0,7" ColumnSpacing="10">
                                            <Frame WidthRequest="42" HeightRequest="42" CornerRadius="21" Padding="0" BackgroundColor="#1A1F2B" BorderColor="Transparent" IsClippedToBounds="True">
                                                <Image Source="{Binding AvatarUrl}" Aspect="AspectFill"/>
                                            </Frame>

                                            <VerticalStackLayout Grid.Column="1">
                                                <Label Text="{Binding DisplayName}" TextColor="#F9FAFB" FontAttributes="Bold"/>
                                                <Label Text="{Binding RoleLabel}" TextColor="#8B94A7" FontSize="12"/>
                                            </VerticalStackLayout>

                                            <Button Grid.Column="2"
                                                    Text="Retirer"
                                                    FontSize="12"
                                                    BackgroundColor="#1A1F2B"
                                                    TextColor="#F9FAFB"
                                                    CornerRadius="12"
                                                    IsVisible="{Binding CanRemove}"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RemoveMemberFromSelectedGroupCommand}"
                                                    CommandParameter="{Binding .}"/>
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>

                            <Label Text="Ajouter des amis" TextColor="#F9FAFB" FontSize="16" FontAttributes="Bold" IsVisible="{Binding CanManageSelectedGroup}"/>

                            <CollectionView ItemsSource="{Binding GroupCandidateFriends}" HeightRequest="180" IsVisible="{Binding CanManageSelectedGroup}">
                                <CollectionView.EmptyView>
                                    <Label Text="Aucun ami à ajouter" TextColor="#6B7280" HorizontalOptions="Center" Margin="0,24"/>
                                </CollectionView.EmptyView>

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="*,Auto" Padding="0,8">
                                            <Label Text="{Binding Profile.Username}" TextColor="#F9FAFB" VerticalOptions="Center"/>

                                            <Button Grid.Column="1"
                                                    Text="Ajouter"
                     
                                                    FontSize="12"
                                                    BackgroundColor="#1A1F2B"
                                                    TextColor="#F9FAFB"
                                                    CornerRadius="12"
                                                    Padding="10,6"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.AddFriendToSelectedGroupCommand}"
                                                    CommandParameter="{Binding .}"/>
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>

                        <!-- PARAMETRES -->
                        <VerticalStackLayout x:Name="GroupSectionParametres" Padding="14" Spacing="12" IsVisible="False">
                            <Label Text="Paramètres du groupe" TextColor="#F9FAFB" FontSize="18" FontAttributes="Bold"/>

                            <Button Text="Changer la photo / bannière" BackgroundColor="#1A1F2B" TextColor="#F9FAFB" CornerRadius="12" Command="{Binding SetSelectedGroupPhotoCommand}"/>
                            <Button Text="Partager le groupe" BackgroundColor="#1A1F2B" TextColor="#F9FAFB" CornerRadius="12" Command="{Binding ShareSelectedGroupCommand}"/>
                            <Button Text="Rafraîchir" BackgroundColor="#1A1F2B" TextColor="#F9FAFB" CornerRadius="12" Command="{Binding LoadSelectedGroupDetailsCommand}"/>
                            <Button Text="Supprimer le groupe" BackgroundColor="#7F1D1D" TextColor="#FEE2E2" CornerRadius="12" IsVisible="{Binding IsSelectedGroupOwner}" Command="{Binding DeleteSelectedGroupCommand}"/>
                        </VerticalStackLayout>

                    </VerticalStackLayout>
                </Frame>

                <!-- PROCHAINE SORTIE EN APERCU -->
                <Frame Margin="16,0,16,24" Padding="16" CornerRadius="22" HasShadow="False" BorderColor="#1F2635" BackgroundColor="#10131B">
                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Text="📅 Prochaines sorties" TextColor="#F9FAFB" FontSize="18" FontAttributes="Bold"/>
                            <Button Grid.Column="1"
                                    Text="Voir tout"
                                    BackgroundColor="Transparent"
                                    TextColor="#C084FC"
                                    FontAttributes="Bold"
                                    Padding="0"
                                    Clicked="OnShowAllOutingsClicked"/>
                        </Grid>

                        <Frame Padding="12" CornerRadius="18" HasShadow="False" BorderColor="#2A3143" BackgroundColor="#121722">
                            <Grid ColumnDefinitions="112,*" ColumnSpacing="14">
                                <Frame WidthRequest="112" HeightRequest="90" CornerRadius="16" Padding="0" IsClippedToBounds="True" BackgroundColor="#1A1F2B" BorderColor="Transparent">
                                    <Label Text="🍸" FontSize="42" HorizontalOptions="Center" VerticalOptions="Center"/>
                                </Frame>

                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label Text="{Binding SelectedGroupNextOutingDate}" TextColor="#C084FC" FontAttributes="Bold"/>
                                    <Label Text="{Binding SelectedGroupNextOutingTitle}" TextColor="#F9FAFB" FontSize="20" FontAttributes="Bold"/>
                                    <Label Text="{Binding SelectedGroupNextOutingBar}" TextColor="#D1D5DB"/>
                                    <Label Text="{Binding SelectedGroupNextOutingStats}" TextColor="#F9FAFB" FontAttributes="Bold"/>
                                </VerticalStackLayout>
                            </Grid>
                        </Frame>
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>